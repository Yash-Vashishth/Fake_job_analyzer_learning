/* Deployment config — edit this one line after you deploy the backend.
   Local development: leave commented out, app.js falls back to localhost. */

// window.API_BASE = "https://your-backend-service.onrender.com";
